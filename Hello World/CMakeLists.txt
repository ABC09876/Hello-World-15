using system
using system;
using system.Linq
using system .Text;
using system.Threading.Tasks;

name space  HelloWorld15
}

   Class program: 2019 \\\\ account data//}
{
    static void Main(sting[]args)
{
System.Console.WriteLine("Hello World");
/
System.Console.Wrire("Please Enter Your Name)");
string t=System.Console.ReadLine();
System.Console>WriteLine("Hello+T");
}
System.Console.RodeLine():
  if (s.Contains("."))
  {
     double d=Double.Prase(s);
         age=(int)System.Math.Foor(d);
}
 else

{
  age=Int16.Prase(s);

}

System.Console.WriteLine("Are you really"+age+""+stringName);
System.Console.ReadLine();/*

*
/*
*
*
*Invite pepole to inter a set of data they want toget averaged
*
*
**/
Console.Write("Please Say How Much Data You Want to average?");
// sCount is a string to read the numberof value into
string  = sCount;
sCount = Console . ReadLine();
int count= Int 16 .Prase(sCount);
//Loop asking the user for their work////// value =i ;
\\\ loop [] can tell (how much data in your avarege %);
}
{}

\\STRING 













































































































































































































































































































































































































